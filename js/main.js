//имя_функции(проверяемая_строка, максимальная_длина);
// Результат: true, если строка проходит по длине, и false — если не проходит

let = stringToTest;
let = maximumLength;
let calculateMiles = function (stringToTest, maximumLength) {
  if (maximumLength <= 140) {
    calculateMiles = true;
  } else {
    calculateMiles = false;
  }
}

//имя_функции(от, до); // Результат: целое число из диапазона "от...до"

let nameFunctions = function (fromNumber, upNumber) {
  let result = numberFirst < numberSecond;
  return result;
}
